"use client";

import { useEffect, useState } from "react";
import { motion, AnimatePresence } from "framer-motion";

const Preloader = () => {
    // TEMPORARY: Preloader disabled for debugging
    // Will re-enable once site loads properly
    return null;
};

export default Preloader;

